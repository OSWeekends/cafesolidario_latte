<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="cafesolidario.osw.es.cafelatte">

    <application
        android:name="es.osw.cafelatte.presentation.ui.CoffeeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="es.osw.cafelatte.presentation.ui.splash.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="eb4b0c36ef2dc0c3aa1dc1ae5cece3317d38e756" />

    </application>

</manifest>
